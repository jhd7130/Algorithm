package algorithmstudy;

public class week1st {

    public static void main(String[] args){
       /*
        [Max Stack]
        기본적인 stack의 룰(push, pop, top)을 따르면서 추가적으로  peekMax, popMax를 지원하는 stack class를 구현해보세요.
        지원하는 methods:
        push(x) - stack에 x를 넣습니다
        pop() - stack의 최상위 element를 삭제하면서 리턴합니다
        top() - stack의 최상위 element를 리턴합니다 (삭제하지 않음)
        peekMax() - stack에서 가장 높은 값을 리턴합니다 (삭제하지 않음)
        popMax() - stack에서 가장 높은 값을 삭제하면서 리턴합니다. 만약 최대값이 하나 이상이면 최상위의 높은 값을 삭제합니다.
        예제:
        algorithmstudy.MaxStack stack = new algorithmstudy.MaxStack();
        stack.push(5);
        stack.push(1);
        stack.push(5);
        stack.top(); // return 5
        stack.popMax(); // return 5
        stack.top(); // return 1
        stack.peekMax(); // return 5
        stack.pop(); // return 1
        stack.top(); // return 5
        제약사항:
        x는 정수이며 수행 회수는 10^4를 초과하지 않습니다.

        */

    }
}
