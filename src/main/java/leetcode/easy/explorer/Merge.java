package leetcode.easy.explorer;

public class Merge {
    public void main(String[] args){
    }
    public void merge(int[] nums1, int m, int[] nums2, int n){
        // 두 배열을 합칠 공간을 생성
        // num1.length = m + n
        // num2.length = n
        // 인덱스 m번까지가 nums1에 적재되어있는 실수
        // 그럼 일단 n부터 num2의 갯수를 직접 넣기
        for(int i = m-n; i< m; i++){
            for( int j = 0; j < n; j++){

            }
        }
        // 받은 배열을 하나씩 arr에 삽입 일단 긴 배열을 먼저 삽입힌다.
        // 우선 0이 들어가 있을지 모르니깐. 각 배열에서 0을 제외하기
    }
}
